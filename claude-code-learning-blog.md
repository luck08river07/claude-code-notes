# Claude Codeを学んでみた！初心者が押さえておきたい基本と使い方まとめ

## はじめに

最近、開発現場で話題の **Claude Code** を学習してみました。
「AIにコードを書かせる」と聞くと身構えてしまう方もいるかもしれませんが、実際に触ってみると、これまでの開発スタイルを大きく変えてくれる頼もしい相棒だと感じました。

この記事では、私が学んだ内容をベースに、**Claude Codeとは何か / 導入方法 / 基本の使い方 / 便利だったTips** を、これから学び始める方にもわかりやすい形でまとめます。

---

## Claude Codeとは？

Claude Code は、Anthropic が提供する **ターミナル上で動くAIコーディングアシスタント** です。
ChatGPTのようにブラウザでチャットするのではなく、**自分のPCのターミナルから直接Claudeと対話しながらコードを書ける** ところが大きな特徴です。

### 主な特徴

- **ターミナル完結型**：エディタを切り替えず、CLIだけで完結
- **ファイルの読み書きができる**：プロジェクト内のファイルを直接読んで、編集や追記までしてくれる
- **コマンド実行ができる**：テスト実行やビルド、Gitコマンドなどもお願いできる
- **長い文脈を保持できる**：プロジェクト全体の流れを踏まえた上で提案してくれる
- **拡張性が高い**：スラッシュコマンドやスキル、MCPサーバーなどを追加して機能を拡張できる

要するに、**「ペアプログラマーがターミナルの中に住んでくれる」** ようなイメージです。

---

## 導入してみよう

### 必要なもの

- Node.js（推奨：最新のLTS版）
- Anthropic のアカウント
- ターミナル（macOS / Linux / Windows のWSLなど）

### インストール手順

公式の手順に沿って、npm でグローバルインストールするのが基本です。

```bash
npm install -g @anthropic-ai/claude-code
```

インストールが終わったら、プロジェクトのフォルダに移動して以下を実行します。

```bash
cd ~/your-project
claude
```

初回起動時は、ブラウザが開いて認証画面に案内されます。
ログインを済ませれば、すぐに対話が始められます。

---

## 基本の使い方

### 1. 対話で指示を出す

起動すると入力欄が表示されるので、自然言語でやってほしいことを伝えるだけです。

```
このリポジトリの構成を教えて
```

```
src/utils/date.ts に、日付を「YYYY年MM月DD日」形式に整形する関数を追加して
```

```
README.mdを読んで、英語の部分を日本語に翻訳して
```

驚いたのは、**「やってほしいこと」だけ伝えれば、ファイルを読み・書き・確認まで自動でやってくれる**こと。
細かい場所を指定しなくても、Claudeが自分で調べて該当箇所を見つけてくれます。

### 2. スラッシュコマンドで操作する

`/` から始まるコマンドで、Claude Codeの動作を制御できます。代表的なものを紹介します。

| コマンド | 用途 |
| --- | --- |
| `/help` | ヘルプを表示 |
| `/clear` | 会話履歴をリセット |
| `/init` | プロジェクト用の `CLAUDE.md`（プロジェクト説明書）を自動生成 |
| `/review` | 変更内容のコードレビュー |
| `/cost` | 使用料金の確認 |

特に `/init` は便利で、**プロジェクトの構成を自動で読み取り、Claudeへの指示書として `CLAUDE.md` を作成**してくれます。これを置いておくと、以降の対話が格段にスムーズになります。

### 3. 計画モード（Plan Mode）

いきなりコードを書かせるのではなく、**まず計画を立ててもらってから実行する** モードです。
`Shift + Tab` で切り替えられます。

「いきなり修正されると怖い…」というときに、まず計画だけ提示してもらい、**承認してから実行**できるので安心です。

---

## 学んで「便利だった！」と感じたポイント

### Tips 1：`CLAUDE.md` を置いておく

プロジェクトのルートに `CLAUDE.md` を置くと、その内容を毎回参照してくれます。

```markdown
# このプロジェクトについて

- 言語：TypeScript
- フレームワーク：Next.js（App Router）
- コーディング規約：ESLint + Prettier
- コミットメッセージ：Conventional Commits
```

このように **ルールや前提を書いておくだけ** で、出力の質が一段上がります。

### Tips 2：曖昧な指示を恐れない

最初は「正確に指示を書かなきゃ」と身構えていましたが、実は **ざっくりした依頼でも十分に動いてくれる** ことが多いです。

例：
> 「このバグ、たぶんログイン処理あたりだと思うんだけど直して」

これだけで、関連ファイルを探して原因を推測し、修正案まで提示してくれることもあります。

### Tips 3：途中で止めて方向修正する

実行中に「あ、それじゃない」と思ったら `Esc` キーで止められます。
**「指示し直す前提で気軽に動かしてみる」** くらいが、ちょうどいい付き合い方だと感じました。

### Tips 4：レビュー役としても使える

自分が書いたコードを Claude にレビューしてもらうのも有効です。

```
src/api/auth.ts のコードに、セキュリティ上の問題がないかレビューして
```

第三者目線で指摘してくれるので、**ペアプロやコードレビュー前のセルフチェック** にぴったりです。

### Tips 5：Gitとの連携が強力

コミットメッセージの自動生成や、PR用のサマリー作成も得意です。

```
変更内容をもとに、Conventional Commits 形式でコミットメッセージを書いて
```

```
このブランチのPR説明文を、背景・変更点・テスト方法に分けて書いて
```

ちょっとしたドキュメント作業を一気に巻き取ってくれるので、心理的負担がかなり軽くなりました。

---

## 使ってみて感じたメリットと注意点

### メリット

- **作業のスピードが上がる**：定型作業や調査の時間が一気に短縮される
- **学習効果がある**：書いてもらったコードを読むことで、新しい書き方や考え方を学べる
- **「面倒」が減る**：README、テスト、コメントなど後回しにしがちな作業が片付く

### 注意したいこと

- **生成結果は必ず自分の目で確認する**：もっともらしいけれど間違っている、ということもある
- **重要な操作は計画モードで確認**：いきなりファイル削除やコミットをされないよう、最初は慎重に
- **機密情報の取り扱いに注意**：APIキーや顧客情報など、扱う情報には常に気を配る

AI任せにするのではなく、**「最終的な責任は自分にある」** という意識で使うのが大切だと感じました。

---

## まとめ

Claude Code を学んでみて、何より良かったのは **「考える時間」が増えた** ことです。

機械的な作業や調べ物をAIに任せることで、本当に集中したい設計や仕様の検討に時間を使えるようになりました。
最初は戸惑うことも多いですが、**まずは小さなタスクから一緒にやってみる** のが一番の近道だと思います。

これから学び始める方は、ぜひ次のステップから試してみてください。

1. インストールして、自分のプロジェクトで `claude` を起動してみる
2. `/init` で `CLAUDE.md` を作成する
3. ちょっとしたタスク（READMEの整理、関数追加など）を依頼してみる
4. 計画モードを使って、安全にコードを修正してみる
5. 慣れてきたら、スラッシュコマンドや拡張機能を試してみる

「AIに開発を任せる」ではなく、**「AIと一緒に開発する」**。
そんな新しい働き方を、ぜひ体験してみてください。

---

*この記事が、これからClaude Codeを学ぶ方の参考になれば嬉しいです。*
*最後まで読んでいただき、ありがとうございました。*
